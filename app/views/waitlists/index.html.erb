<div class="container" style="padding: 20px">
  <% if @waitlist %>
    <% if @waitlist.position <= 0 %>
      <h1>Congrats, you have moved up the waitlist! </h1>
      <%= link_to "Sign up anytime here", new_user_registration_path %>
    <% else %>
      <h4 class="waitlist-position-text">
        <%= "You are number #{@waitlist.position} out of #{Waitlist.last_position} on the waitlist to get access. Don't forget to share with friends and get access sooner! Check back here anytime to see your position. Thank you!" %>
      </h4>
      <p><%= "Your referral link: #{@waitlist.referral_link}" %></p>
      <p class="share-waitlist-text">Share on social media</p>
      <span>

        <%= link_to facebook_url(@waitlist), class: "facebook", rel: "noopener", target: "_blank" do %>
          <span class="fa fa-facebook fa-2x"></span>
        <% end %>
        <%= link_to tweet_url(@waitlist), class: "twitter twitter-waitlist", rel: "noopener", target: "_blank" do %>
          <span class="fa fa-twitter fa-2x"></span>
        <% end %>
      </span>
      <p class="share-waitlist-text">Improve your position by signing up your friends.</p>
    <% end %>
  <% else %>
    <center>
      <h3>Looks like you haven't joined the waitlist yet</h3>
      <a href="#" data-toggle="modal" data-target="#waitlistModal" style="margin-left:40px;font-size:1.15em;" class="btn btn-info login-button">Sign up here</a>    
    </center>
  <% end %>
</div>
